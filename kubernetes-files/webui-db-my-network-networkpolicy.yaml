apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: webui-db-my-network
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/webui-db-my-network: "true"
  podSelector:
    matchLabels:
      io.kompose.network/webui-db-my-network: "true"
